# Set this to the canonical production origin to enable canonical URLs and sitemap output.
PUBLIC_SITE_URL=https://example.com

# Optional read-only token used at build time to refresh public GitHub project data.
GITHUB_TOKEN=
